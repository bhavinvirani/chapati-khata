// ─────────────────────────────────────────────────────────────
// The only things you change to run this for your group.
// Names are the allowlist for the sign-in gate (case-insensitive).
// ─────────────────────────────────────────────────────────────

export const ALLOWED_NAMES = [
  "bhavin",
  "abhishek",
  "deven",
  "parth",
  "pratik",
  "hitanshi",
  "samir",
];

export const DEFAULT_PRICE = 0.5; // price per chapati at the default rate
export const CURRENCY = "$";
